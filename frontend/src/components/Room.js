import { useState } from "react";
import axios from "axios";

function Room() {
  return (
    <div className="container">
      Welcome
      {/* Welcome ${userId1} and ${userId2} to room ${roomId}! */}
    </div>
  );
}

export default Room;
